<div class="author">
    <h1 class="author__heading">Hi, this is Beauty Blog.</h1>
    <p class="author__body">Web developer,content creator and author of this blog. Welcome to a site where we talk all things beauty. I know this sounds shallow. The beauty talks we have will dive below the surface broadening our knowledge of beauty beyond the physical. Join me on this journey of discovery.</p>
</div>

<img src="./public/img/beauty photo.jpg" alt="Beauty Photo" class="hero-image" width="981" height="528">

<section class="articles">
    <h2 class="articles__heading">Latest Posts</h2>

    <ul class="article-ul">
        <% data.forEach(post=> { %>
            <li>
                <a href="/post/<%= post._id %>">
                    <span>
                        <%= post.title %>
                    </span>
                    <span class="article-list__date">
                        <%= post.createdAt.toDateString() %>
                    </span>
                </a>
            </li>
            <% }) %>
    </ul>


    <% if (nextPage !==null) { %>
        <a href="/?page=<%= nextPage %>" class="pagination">&lt; View Older Posts</a>
        <% } %>
</section>